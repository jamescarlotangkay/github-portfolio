#this is a simple calculator i made
#second project
#second day of learning python
#no help of chatgpt, only pycharm recommendations

while True:
    formula = input('What would you like to do? (ADDITION, SUBTRACTION, MULTIPLICATION, DIVISION, PERCENTAGE, EXPONENT) ').upper()
    if formula == 'ADDITION' or formula == 'SUBTRACTION':
        break
    if formula == 'MULTIPLICATION' or formula == 'DIVISION':
        break
    if formula == 'PERCENTAGE'or formula == 'EXPONENT':
        break
    print('INVALID INPUT')

while True:
    first_number = input('What is the first number? ')
    if first_number == float or int:
        break
    print('INVALID INPUT')

while  True:
    second_number = input('What is the second number ?' )
    if second_number == float or int:
        break
    print('INVALID INPUT')

sum=float(first_number)+float(second_number)
difference=float(first_number)-float(second_number)
product=float(first_number)*float(second_number)
quotient=float(first_number)/float(second_number)
percentage=float(first_number)%float(second_number)
exponent=float(first_number)**float(second_number)


if formula == 'ADDITION':print(sum)
if formula == 'SUBTRACTION':print(difference)
if formula == 'MULTIPLICATION':print(product)
if formula == 'DIVISION':print(quotient)
if formula == 'PERCENTAGE':print(percentage)
if formula == 'EXPONENT':print(exponent)

